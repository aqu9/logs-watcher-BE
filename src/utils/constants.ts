import * as path from 'path';

export const logFolderPath = path.join(__dirname, '../..', 'logs');

export const logFilePath = path.join(logFolderPath, 'logs.txt');
export const noOfLinesTofetch = 10;
